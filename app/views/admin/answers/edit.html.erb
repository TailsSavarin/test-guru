<h2><%= t('.header') %></h2>

<p>
  <strong><%= t('.body') %></strong>
  <%= @answer.question.body %>
</p>

<%= render "form" %>

<p><%= link_to t('.back'), admin_question_path(@answer.question) %></p>
