<tr>
  <td><%= answer.body %></td>
  <td>
    <div class="btn-group" role="group" aria-label="Basic example">
      <%= link_to t('.show'), admin_answer_path(answer), class: 'btn btn-outline-secondary' %>
      <%= link_to t('.edit'), edit_admin_answer_path(answer), class: 'btn btn-outline-secondary' %>
      <%= link_to t('.delete'), admin_answer_path(answer), method: :delete, data: { confirm: t('.confirm')}, class: 'btn btn-outline-secondary' %>
    </div>
  </td>
</td>
