<h1><%= t('.header') %></h1>

<table class="table table-hover table-sm">
  <thead class="thead-dark">
    <tr>
      <th><%= t('.question') %></th>
      <th><%= t('.gist_url') %></th>
      <th><%= t('.user') %></th>
    </tr>
  </thead>
  <tbody>
    <%= render @gists %>
  </tbody>
</table>

<p><%= link_to t('.back'), admin_tests_path, class: 'btn btn-secondary btn-block' %></p>
