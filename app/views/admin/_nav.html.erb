<div class="nav nav-admin">
  <span class="nav-admin title"><%= t('.title') %></span>

  <div class="nav-admin user">
    <%= t('.admin') %>
    <%= admin_status %> |
    <%= t('.login_time') %>
    <%= current_time %> |
    <%= t('.profile', email: current_user.email) %></b> |
    <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
  </div>
</div>
